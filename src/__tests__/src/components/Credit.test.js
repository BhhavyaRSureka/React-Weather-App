// ********RoostGPT********
// Test generated by RoostGPT for test ReactTesting using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render, cleanup ,screen} from '@testing-library/react';
import Credit from '../../../components/Credit';
import '@testing-library/jest-dom/extend-expect';

describe('Credit component', () => {
  afterEach(cleanup);

  // Test case 1: Renders without crashing
  test('renders without crashing', () => {
    const { container } = render(<Credit />);
    expect(container).toBeTruthy();
  });

  // Test case 2: Contains the correct information
  test('contains the correct text content', () => {
    const { getByText } = render(<Credit />);
    expect(getByText(/coded by/i)).toBeInTheDocument();
    expect(getByText(/Open sourced on Github/i)).toBeInTheDocument();
    expect(getByText(/Hosted on Netlify/i)).toBeInTheDocument();
  });

  // Test case 3: Checks the links and URLs
  test('has correct links and URLs', () => {
    const { getByText } = render(<Credit />);
    const sheCodesLink = getByText('Chaimae Lamirine').closest('a');
    const githubLink = getByText('Open sourced on Github.').closest('a');
    
    expect(sheCodesLink).toHaveAttribute('href', 'https://www.shecodes.io/graduates/73033-chaimae-lamirine');
    expect(githubLink).toHaveAttribute('href', 'https://github.com/shemmee/React-Weather-App');
  });

  // Additional or alternative tests can be written based on further requirements or more complex logic.
});


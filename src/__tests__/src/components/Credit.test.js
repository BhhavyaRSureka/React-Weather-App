// ********RoostGPT********
// Test generated by RoostGPT for test ReactTesting using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import Credit from '../../../components/Credit'; // Adjusted relative path

describe('Credit Component', () => {
  test('renders the correct text', () => {
    const { getByText } = render(<Credit />);
    expect(getByText('Coded by')).toBeInTheDocument();
  });

  test('contains the correct links', () => {
    const { getByText } = render(<Credit />);
    const graduateLink = getByText('Chaimae Lamirine');
    expect(graduateLink).toBeInTheDocument();
    expect(graduateLink).toHaveAttribute('href', 'https://www.shecodes.io/graduates/73033-chaimae-lamirine');

    const githubLink = getByText('Open sourced on Github.');
    expect(githubLink).toBeInTheDocument();
    expect(githubLink).toHaveAttribute('href', 'https://github.com/shemmee/React-Weather-App');
  });

  // Add more test cases as needed to cover various scenarios and edge cases
});


// ********RoostGPT********
// Test generated by RoostGPT for test ReactTesting using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render, screen } from '@testing-library/react';
import Credit from 'src/components/Credit';

describe('Credit Component Tests', () => {
  test('renders without crashing', () => {
    render(<Credit />);
    expect(screen.getByText(/coded by/i)).toBeInTheDocument();
  });

  test('displays the correct credit message', () => {
    render(<Credit />);
    expect(screen.getByText(/coded by/i)).toBeInTheDocument();
    expect(screen.getByText(/chaimae lamirine/i)).toBeInTheDocument();
    expect(screen.getByText(/open sourced on github/i)).toBeInTheDocument();
    expect(screen.getByText(/hosted on netlify/i)).toBeInTheDocument();
  });

  test('links point to the correct URLs', () => {
    render(<Credit />);
    const shecodesLink = screen.getByText(/chaimae lamirine/i).closest('a');
    const githubLink = screen.getByText(/open sourced on github/i).closest('a');
    
    expect(shecodesLink).toHaveAttribute('href', 'https://www.shecodes.io/graduates/73033-chaimae-lamirine');
    expect(githubLink).toHaveAttribute('href', 'https://github.com/shemmee/React-Weather-App');
  });
});

